# FastAPI & Server
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
websockets==12.0  # Phase 27: WebSocket support

# Database
sqlalchemy==2.0.23
pymysql==1.1.0
cryptography==41.0.7

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.0
email-validator==2.1.0

# ML & Data Science
scikit-learn==1.3.2
pandas==2.1.3
numpy==1.26.2
joblib==1.3.2
shap==0.42.1

# Differential Privacy (Phase 13+)
opendp==0.9.2
opacus==1.4.0  # Formal DP-SGD with per-sample clipping and RDP accounting

# PyTorch & TFT (MANDATORY for TFT-based multi-horizon forecasting per paper)
torch==2.1.1
pytorch-forecasting==1.0.0
pytorch-lightning==2.1.0

# Web3 for Blockchain
web3==6.11.0

# Utilities
python-dotenv==1.0.0
pydantic==2.5.0
pydantic-settings==2.1.0
setuptools>=65.0.0

# Monitoring (Phase 37)
psutil==5.9.6
prometheus-client==0.19.0

# File handling
aiofiles==23.2.1

# LLM Integration (Phase 13+: Copilot Enhancement + Gemini AI Summarization)
google-generativeai==0.3.1  # Gemini API for AI-powered prediction summaries
reportlab==4.0.7  # PDF generation for prediction reports
matplotlib==3.8.2  # Chart generation for reports
transformers==4.36.0  # HuggingFace Transformers for lightweight LLMs
accelerate==0.25.0    # Optimized model loading and device management
bitsandbytes==0.42.0  # 4-bit quantization (GPU acceleration, optional)
sentencepiece==0.1.99  # Tokenizer for some models (LLaMA, Mistral)
